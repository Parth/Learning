#include <stdio.h>
#include <stdlib.h>

#include "variable.h"

#define true 1
#define false 0

get_nu_of_temp_variables(char** instructions, n_instructions) {
	for 
		for
			if strlen 1
				if lowercase
					num++

	return num;
}

run_program(var** inputs, var** outputs, instructions) {
	var** temp variables = get_nu_of_temp_variables(instructions);
	for (instruction) {
		execute(inputs, outputs, temps, instruction)
	}
	print output values
}

int main(int argc, char** args) {
	for each input: 
		run_program(inputs, outputs, instructions)
}

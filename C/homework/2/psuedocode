Trie psuedocode

master {
	node* nodes;
	int nodes_count;
}

node {
	char* word;
	int isWord;
	node* rest;
	int rest_count;
}

to_string_helper(node* node, int level) {
	for (level) {
		printf(tab)
	}

	if (node == NULL) {
		return;
	}

	print(node -> word)

	for (node -> rest_count) {
		to_string_helper(rest[i], level++);
	}

	return;
}

to_string(master* master) {
	for (master -> n) {
		to_string_helper(master -> children[i], 1);
	}
}

insert
